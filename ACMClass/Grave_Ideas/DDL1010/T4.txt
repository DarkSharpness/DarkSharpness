1.不一样,因为c的函数寄存器i的初始值应该为 (e^step - 1)^(8-i)
其中step为步长,所以当step 从 0.01 变为 1 以后,答案改变

2.不适合,但是有可能可以做。
考虑一个寄存器,其只保留小数部分,超过的部分直接溢出不处理.
然后,我们只需把x^2对应的寄存器的值只保留小数部分即可，在计算的过程中整数部分自动溢出并不认为是异常即可。
除此之外，我们还有其他的解决方案，x = [x] + {x} 所以 {x*x} = {x}*[x]*2 + {x}*{x}