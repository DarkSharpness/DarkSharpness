#include <Dark/dynamic_array>
#include <bits/stdc++.h>
using dark::dynamic_array;

template <class T>
void print(const T &A) {
    for(auto &it : A) std::cout << it << ' ';
    std::cout << '\n';
}

signed main() {
    dynamic_array <int> A;
    return 0;
}