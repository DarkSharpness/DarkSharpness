#include <bits/stdc++.h>

template <class T>
class TD {};

signed main() {
    const int theAnswer = 42;
    auto x = theAnswer;
    auto y = &theAnswer;
    TD <decltype(x)> xType;
    TD <decltype(y)> yType;
    std::cout << sqrt(15) * 32;
    return 0;
}