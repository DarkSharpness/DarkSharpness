#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

void work(int n,int m)

int main() {
    ios::sync_with_stdio(false);
    int T;
    cin >> T;
    while(T--) {
        ll n,m;    
        cin >> n >> m;
        work(n,m);
    }    
    return 0;
}
