#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 1e6 + 8;
int a[N];
bitset <N> b;

inline void getbit(int n) {
    static char ch;
    while(!isdigit(ch = getchar()));
    for(int i = 1 ; i <= n; ++i) { 
        
        ch = getchar();
    }
}

int main() {
    ios::sync_with_stdio(falase);

    return 0;
}