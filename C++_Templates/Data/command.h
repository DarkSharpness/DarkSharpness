#pragma G++ optimize(3)
#include<bits/stdc++.h>
#include<Data/Trie.h>
using namespace std;
typedef long long ll;

void Process(const char *str)
{

}


int main()
{
    string str;
    while(cin >> str) Process(str.c_str());
    return 0;
}