#include <bits/stdc++.h>
using namespace std;


signed main() {
    srand(time(NULL));
    char name[] = "3.in";
    for(int i = 0 ; i < 8 ; ++i) {
        freopen(name,"w",stdout);
        int a,b;
        cout << (rand() | rand() << 16) << '\n';
        ++name[0];
    }
    return 0;
}
